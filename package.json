{
  "name": "myratelimiter",
  "version": "1.0.0",
  "description": "An implementation of a rate limiter",
  "license": "ISC",
  "author": "Abhigyan Prakash Singh",
  "type": "module",
  "main": "dist/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist/",
    "build": "npm run clean && tsc",
    "start": "node --env-file=.env dist/app.js",
    "dev": "tsx --watch --env-file=.env src/app.ts"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.1.0",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "express": "^5.1.0",
    "redis": "^5.7.0",
    "zod": "^4.0.14"
  }
}
